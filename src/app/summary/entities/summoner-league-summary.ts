// import { League } from 'src/app/league/entities/league.entity';
// import { Summoner } from 'src/app/summoner/entities/summoner.entity';
// import {
//   Column,
//   Entity,
//   //   JoinColumn,
//   ManyToOne,
//   OneToMany,
//   PrimaryGeneratedColumn,
// } from 'typeorm';

// @Entity()
export class SummonerLeagueSummary {
  // @PrimaryGeneratedColumn('uuid')
  // uuid: string;
  // @Column()
  // summonerId: string;
  // @ManyToOne(() => Summoner, (summoner) => summoner.id)
  // //   @JoinColumn({ name: 'summonerId' })
  // summoner: Summoner;
  // @OneToMany(() => League, (league) => league.summonerId)
  // leagues: League[];
}
