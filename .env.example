# ===========================================
# VOID Backend - Environment Configuration
# ===========================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# NEVER commit .env with real credentials to git!

# === DATABASE ===
# PostgreSQL credentials (must match void-postgres container)
POSTGRES_USER=void
POSTGRES_PASSWORD=void
POSTGRES_DB=void

# === RIOT API ===
# REQUIRED! Get your API key from https://developer.riotgames.com/
# 
# Development API Keys:
#   - Expire every 24 hours
#   - Rate limited to 20 requests per second
#   - Good for testing
#
# Production API Keys:
#   - Require approval from Riot Games
#   - Higher rate limits
#   - Required for public deployment
#
RIOT_API_KEY=RGAPI-xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# === FRONTEND ===
# URL where the backend API is accessible from the browser
# 
# Local development:
VITE_API_URL=http://localhost:4000
#
# Docker internal (if using nginx proxy):
# VITE_API_URL=/api
#
# Production (your domain):
# VITE_API_URL=https://api.your-domain.com
